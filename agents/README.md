# Agents

Cada tarefa recorrente deve possuir um `agent-id` estável.

Padrão:
`account-<NN>-<role>`

Cada agente mantém:
`agents/<agent-id>/status.md`
`agents/<agent-id>/worklog.md`

Evite editar arquivos de outro agente.
